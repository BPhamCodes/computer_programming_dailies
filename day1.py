# write a function that traverses a list and a random name from that list
# bonus if you write in nested loops for traversal, concetanation of results, etc.
import random

list1 = ['A', 'B', 'C']

def gen_rand_numb(list):
    random_choice_1 = random.choice(list)
    print(random_choice_1)

gen_rand_numb(numbers)
#print (gen_rand_numb(numbers))